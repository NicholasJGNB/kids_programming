# 改了什么

简单描述这个 PR 做了什么、为什么。

关联 issue：#（如果有）

## 自检清单

- [ ] 本地能正常运行（`npm run serve` 后浏览器打开试过）
- [ ] 如果改了关卡：跑过 `npm run eval`，所有关卡仍可解
- [ ] 如果改了逻辑：跑过 `npm test` 通过
- [ ] 保持了"零依赖、双击即玩"的原则（没有引入需要构建的依赖）
- [ ] 面向孩子的文案简单、友好

## 截图（可选）

如果是界面改动，贴个前后对比图会很有帮助。
